# 股票代码标准化修复说明

## 问题描述

在使用TradingAgents框架时，发现`results`目录下存在重复的股票代码文件夹，例如：
- `002241/` 和 `002241.sz/`
- `002475/` 和 `002475.sz/`

这是因为用户在不同时间运行CLI时，输入了不同格式的股票代码（带`.sz`后缀和不带后缀），导致系统创建了重复的文件夹。

## 修复方案

### 1. CLI层面标准化（主要修复）

**文件**: `cli/main.py`

- 新增 `normalize_ticker()` 函数，用于标准化股票代码格式
- 修改 `get_ticker()` 函数，在用户输入后自动标准化股票代码
- 支持的标准化规则：
  - 中国股票代码（6位数字）：移除`.sz`、`.SZ`、`.sh`、`.SH`后缀
  - 美股代码：保持原格式，转为大写

**示例**:
```python
# 用户输入 "002241.sz" -> 标准化为 "002241"
# 用户输入 "AAPL" -> 保持为 "AAPL"
# 用户输入 "nvda" -> 标准化为 "NVDA"
```

### 2. TradingAgentsGraph层面标准化（双重保险）

**文件**: `tradingagents/graph/trading_graph.py`

- 在 `TradingAgentsGraph` 类中新增 `_normalize_ticker()` 方法
- 修改 `propagate()` 方法，在处理前先标准化股票代码
- 确保即使绕过CLI直接调用API，也能保持一致性

### 3. 重复文件夹合并工具

**文件**: `merge_duplicate_folders.py`

提供了一个独立的脚本来处理现有的重复文件夹：
- 自动检测重复的股票代码文件夹
- 智能合并文件，保留较新的版本
- 自动备份原始数据
- 安全删除重复文件夹

## 使用方法

### 运行合并脚本（处理现有重复文件夹）

```bash
# 在项目根目录下运行
python merge_duplicate_folders.py
```

脚本会：
1. 扫描 `results/` 目录查找重复文件夹
2. 显示发现的重复组
3. 询问用户确认
4. 创建备份到 `backups/` 目录
5. 合并文件夹并删除重复项

### 验证修复效果

```bash
# 运行测试脚本验证标准化功能
python test_ticker_normalization.py
```

### 正常使用CLI

修复后，CLI会自动标准化用户输入：

```bash
# 运行CLI
python -m cli.main

# 当用户输入 "002241.sz" 时，系统会显示：
# Info: Ticker standardized from '002241.sz' to '002241'
# 并创建 results/002241/ 文件夹
```

## 标准化规则

### 中国股票代码
- **输入**: `002241`, `002241.sz`, `002241.SZ`
- **标准化后**: `002241`
- **文件夹**: `results/002241/`

### 美股代码
- **输入**: `AAPL`, `aapl`
- **标准化后**: `AAPL`
- **文件夹**: `results/AAPL/`

### 其他格式
- 保持原有格式，转为大写
- 支持包含点号和连字符的代码

## 文件清单

### 修改的文件
1. `cli/main.py` - 添加CLI层面的股票代码标准化
2. `tradingagents/graph/trading_graph.py` - 添加图层面的标准化

### 新增的文件
1. `merge_duplicate_folders.py` - 重复文件夹合并工具
2. `test_ticker_normalization.py` - 标准化功能测试脚本
3. `TICKER_NORMALIZATION_FIX.md` - 本说明文档

## 注意事项

1. **备份重要**: 运行合并脚本前，建议手动备份重要数据
2. **测试验证**: 修复后建议先用测试数据验证功能
3. **配置兼容**: 修复不影响现有配置文件和API接口
4. **向后兼容**: 现有的分析结果和日志文件不受影响

## 预期效果

修复完成后：
- ✅ 不会再产生重复的股票代码文件夹
- ✅ 用户输入更加灵活（支持带后缀和不带后缀）
- ✅ 系统行为更加一致和可预测
- ✅ 现有重复文件夹可以安全合并

## 故障排除

如果遇到问题：

1. **合并脚本失败**: 检查 `results/` 目录权限，确保有读写权限
2. **标准化不生效**: 运行测试脚本检查函数是否正常工作
3. **CLI显示异常**: 检查是否正确修改了 `cli/main.py` 文件

如需回滚修改，可以从备份目录恢复原始文件。